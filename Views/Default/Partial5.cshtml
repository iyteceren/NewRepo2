@using TravelTripProje.Models.Siniflar
@model List<Blog>

   
    <div class="box1-left">
        @foreach (var x in Model)
        {
            <div class="bot-img">
                <img src="@x.BlogImage" alt="" style="width:120px; height:75px;" />
            </div>
            <div class="bot-desc1">
                <p>@x.Açıklama.Substring(0, 15) ...</p>
            </div>
            <div class="clear"></div>
        }


    </div>

